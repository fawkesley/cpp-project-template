# Your README file goes here

To build, type te following:
    
    $ mkdir build
    $ cd build
    $ ccmake ../src # set the release type, defaults to 'RelWithDebInfo'
    $ cmake ../src
    $ make
